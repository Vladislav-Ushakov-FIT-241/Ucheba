using System;
namespace savage
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("Введите строки");
            int count = 0;
            int pred = 0;
            int lena = 0;
            while (true)
            {
                string line0 = Console.ReadLine();
                if (line0 == "")
                {
                    break;
                }
                string line = string.Empty;
                char lastChar = '\0';
                
                foreach (char c in line0)
                {
                    if (c != lastChar)
                    {
                        lena = 0;
                        line += c;
                        lastChar = c;
                    }
                    else
                    {
                        lena++;
                        if (lena < 2)
                        {
                            line += c;
                        }
                    }
                }

                int ai = line.IndexOf('a');
                int bi  = line.IndexOf('b');
                if (ai == bi - 2)
                {
                    count++;
                    Console.WriteLine("podxodit");
                }
                
                else
                {
                    int ia = -1;
                    int ib = -1;
                    for (int i = 0; i < line.Length; i++)
                    {
                        if (line[i] == 'a')
                            ia = i;
                        else if (line[i] == 'b')
                            ib = i;
                        if (ia == ib - 2)
                        {
                            count++;
                            Console.WriteLine("podxodit");
                        }
                    }
                }
                
                
            }
            Console.WriteLine(count);
        }
    }
}
